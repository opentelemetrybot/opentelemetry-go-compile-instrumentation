set_reader_source:
  target: main
  where:
    struct_literal: io.LimitedReader
  do:
    - set_fields:
        field:
          - name: R
            value: 'strings.NewReader("hello")'
  imports:
    strings: "strings"
