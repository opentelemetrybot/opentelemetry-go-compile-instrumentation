wrap_println:
  target: main
  where:
    function_call: fmt.Println
  do:
    - wrap_call:
        replace: |-
          (func() (int, error) {
            println("handler arg:", {{ .FuncArgument 0 }})
            return {{ . }}
          })()
